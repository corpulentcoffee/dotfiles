[alias]
	graph = ! log git-graph
	staged = diff --staged
	up = fetch --all --prune --tags --verbose

[rebase]
	autoSquash = true
	autoStash = true

[user]
	name = Dave Shifflett
	email = dave@corpulent.coffee
