/**
 * These extensions are generally used by global defaults in the home-installed
 * `../home/.config/Code/User/settings.json`, so all of them should probably be
 * enabled globally in Visual Studio Code.
 */
{
  "recommendations": [
    /**
     * Provide autoformatting for markdown, JSON, YAML, and some others. See
     * `.prettierrc.yml` for options controlling autoformatting behavior.
     */
    "esbenp.prettier-vscode",

    /** Provide autoformatting for shell scripts and some others. */
    "foxundermoon.shell-format",

    /**
     * Autoformatting, typechecking and more. Automatically enables Pyright by
     * installing the `ms-python.vscode-pylance` extension. The Python extension
     * will also install some other Jupyter-related extensions, but those can be
     * disabled without affecting the Python functionality.
     */
    "ms-python.python"
  ]
}
